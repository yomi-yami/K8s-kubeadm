kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc1
  namespace: argo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-client
